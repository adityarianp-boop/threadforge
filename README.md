# ThreadForge

Premium Threads content intelligence platform scaffold.

## Setup

1. Copy `.env.example` to `.env.local`
2. Install deps: `npm install`
3. Run: `npm run dev`

## TODO
- Wire Clerk middleware/auth guards
- Persist generations and ideas in Supabase
- Add shadcn/ui CLI components
